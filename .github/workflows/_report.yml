name: _Report (assign levels + generate report + renv.lock)

on:
  workflow_call:
    inputs:
      run-validate:
        description: "Whether the validate job ran (true) or was skipped (false)"
        type: string
        required: false
        default: 'true'
      run-riskmetric:
        description: "Whether the riskmetric job ran (true) or was skipped (false)"
        type: string
        required: false
        default: 'true'
      run-scan:
        description: "Whether the scan job ran (true) or was skipped (false)"
        type: string
        required: false
        default: 'true'
  workflow_dispatch:

jobs:
  report:
    runs-on: windows-latest
    permissions:
      actions: write
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      GITHUB_TOKEN: ${{ github.token }}
      CACHE_VERSION: ${{ vars.CACHE_VERSION || 'v2' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup R environment
        id: r-env
        uses: ./.github/actions/setup-r-environment

      - name: Setup TinyTeX
        uses: r-lib/actions/setup-tinytex@v2

      - name: Update TinyTeX and rebuild formats
        shell: pwsh
        run: |
          tlmgr update --self --all
          fmtutil-sys --all

      - name: Assert R package cache hit
        if: steps.r-env.outputs.cache-hit != 'true'
        shell: bash
        run: |
          echo "::error::R package cache was not found. Re-run the setup job or increment CACHE_VERSION in repo variables to rebuild."
          exit 1

      - name: Restore validation cache
        if: inputs.run-validate != 'true' || inputs.run-riskmetric != 'true' || inputs.run-scan != 'true'
        id: vcache-restore
        uses: actions/cache/restore@v5
        with:
          path: _vcache
          key: validation-${{ env.CACHE_VERSION }}-${{ hashFiles('r-packages.txt') }}-${{ github.run_id }}
          restore-keys: |
            validation-${{ env.CACHE_VERSION }}-${{ hashFiles('r-packages.txt') }}-

      - name: Copy cached results for skipped jobs
        if: inputs.run-validate != 'true' || inputs.run-riskmetric != 'true' || inputs.run-scan != 'true'
        shell: bash
        run: |
          if [[ "${{ inputs.run-validate }}" != "true" ]]; then
            if [[ ! -f "_vcache/validation-results.json" ]]; then
              echo "::error::validation-results.json not in cache but validate job was skipped"
              exit 1
            fi
            cp _vcache/validation-results.json validation-results.json
            echo "Using cached validation-results.json"
          fi
          if [[ "${{ inputs.run-riskmetric }}" != "true" ]]; then
            if [[ ! -f "_vcache/riskmetric-results.json" ]]; then
              echo "::error::riskmetric-results.json not in cache but riskmetric job was skipped"
              exit 1
            fi
            cp _vcache/riskmetric-results.json riskmetric-results.json
            echo "Using cached riskmetric-results.json"
          fi
          if [[ "${{ inputs.run-scan }}" != "true" ]]; then
            if [[ ! -f "_vcache/vulnerability-report.csv" ]]; then
              echo "::error::vulnerability-report.csv not in cache but scan job was skipped"
              exit 1
            fi
            cp _vcache/vulnerability-report.csv vulnerability-report.csv
            echo "Using cached vulnerability-report.csv"
          fi

      - name: Download setup artifacts
        uses: actions/download-artifact@v4
        with:
          name: setup-artifacts

      - name: Download riskmetric artifacts
        if: inputs.run-riskmetric == 'true'
        uses: actions/download-artifact@v4
        with:
          name: riskmetric-artifacts

      - name: Download scan artifacts
        if: inputs.run-scan == 'true'
        uses: actions/download-artifact@v4
        with:
          name: scan-artifacts

      - name: Download all per-package validation artifacts
        if: inputs.run-validate == 'true'
        uses: actions/download-artifact@v4
        with:
          pattern: validation-*
          merge-multiple: true

      - name: Merge per-package validation results
        if: inputs.run-validate == 'true'
        shell: Rscript {0}
        run: |
          library(jsonlite)

          # Exclude validation-results.json itself — it matches the same pattern
          # and may already exist in the workspace from a previous run's artifact.
          val_files <- list.files(pattern = "^validation-.+\\.json$")
          val_files <- val_files[val_files != "validation-results.json"]
          message("Found ", length(val_files), " per-package validation file(s): ",
                  paste(val_files, collapse = ", "))

          if (length(val_files) == 0) {
            stop("No per-package validation artifacts found. Ensure the validate matrix jobs ran and uploaded their artifacts.")
          }

          all_results <- list()
          for (f in val_files) {
            pkg_results <- fromJSON(f, simplifyVector = FALSE)
            all_results <- c(all_results, pkg_results)
          }

          if (length(all_results) == 0) {
            stop("Per-package validation files were found but all are empty.")
          }

          writeBin(
            charToRaw(enc2utf8(as.character(toJSON(all_results, pretty = TRUE, auto_unbox = TRUE)))),
            "validation-results.json"
          )
          message("Merged ", length(all_results), " package(s) into validation-results.json")

      - name: Assign validation levels
        shell: Rscript {0}
        run: source("scripts/assign-levels.R")

      - name: Capture session info
        shell: Rscript {0}
        run: |
          si <- sessionInfo()
          writeLines(capture.output(print(si)), "session-info.txt")
          message("Session info captured")

      - name: Generate HTML validation report
        shell: Rscript {0}
        run: source("scripts/generate-report.R")

      - name: Generate PDF validation report
        shell: Rscript {0}
        run: source("scripts/generate-pdf-report.R")

      - name: Create renv.lock
        shell: Rscript {0}
        run: |
          lib_path <- .libPaths()[1]
          installed_pkgs <- installed.packages(lib.loc = lib_path)[, 'Package']

          exclude_pkgs <- c('renv', 'pak', 'base', 'compiler', 'datasets', 'graphics',
                           'grDevices', 'grid', 'methods', 'parallel', 'splines',
                           'stats', 'stats4', 'tcltk', 'tools', 'translations', 'utils')
          pkgs_to_snapshot <- setdiff(installed_pkgs, exclude_pkgs)

          renv::snapshot(
            library = lib_path,
            packages = pkgs_to_snapshot,
            prompt = FALSE,
            force = TRUE
          )

      - name: Stage validation cache
        if: inputs.run-validate == 'true' || inputs.run-riskmetric == 'true' || inputs.run-scan == 'true'
        shell: bash
        run: |
          mkdir -p _vcache
          cp validation-results.json _vcache/validation-results.json
          cp riskmetric-results.json _vcache/riskmetric-results.json
          cp vulnerability-report.csv _vcache/vulnerability-report.csv
          echo "Staged validation cache"

      - name: Save validation cache
        if: inputs.run-validate == 'true' || inputs.run-riskmetric == 'true' || inputs.run-scan == 'true'
        uses: actions/cache/save@v5
        with:
          path: _vcache
          key: validation-${{ env.CACHE_VERSION }}-${{ hashFiles('r-packages.txt') }}-${{ github.run_id }}

      - name: Upload validation artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: validation-artifacts
          path: |
            validation-report.html
            validation-report.pdf
            system-info.json
            package-classification.json
            validation-results.json
            riskmetric-results.json
            vulnerability-report.csv
            level-assignments.json
            session-info.txt
            renv.lock

      - name: Delete intermediate artifacts
        if: success()
        shell: pwsh
        run: |
          $artifacts = (gh api "repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts?per_page=100" | ConvertFrom-Json).artifacts
          foreach ($artifact in $artifacts) {
            if ($artifact.name -ne "validation-artifacts") {
              Write-Host "Deleting artifact: $($artifact.name)"
              gh api --method DELETE "repos/${{ github.repository }}/actions/artifacts/$($artifact.id)"
            }
          }
